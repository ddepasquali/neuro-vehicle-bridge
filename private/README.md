# private

Asset proprietari e SDK locali.

- `vendor/muse/Muse.framework`: framework Muse (macOS) usato dal bridge in `backend/bridges/muse/` (symlink già puntato qui).

Non aggiungere altri file sensibili senza encrypt o consenso; questa cartella serve solo per binari/SDK indispensabili.
